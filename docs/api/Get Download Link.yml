info:
  name: Get Download Link
  type: http
  seq: 10

http:
  method: GET
  url: "{{BASE_URL}}/resources/download/:id"
  headers:
    - name: Authorization
      value: Bearer {{AccessToken}}
  params:
    - name: id
      value: "19"
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
