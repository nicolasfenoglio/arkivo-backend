info:
  name: Get Note
  type: http
  seq: 9

http:
  method: GET
  url: "{{BASE_URL}}/notes/:id"
  headers:
    - name: Authorization
      value: Bearer {{AccessToken}}
  params:
    - name: id
      value: "3"
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
