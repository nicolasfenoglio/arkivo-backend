info:
  name: Get Profile
  type: http
  seq: 5

http:
  method: GET
  url: "{{BASE_URL}}/profiles/1"
  headers:
    - name: Authorization
      value: Bearer {{AccessToken}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
