info:
  name: My Profile
  type: http
  seq: 3

http:
  method: GET
  url: "{{BASE_URL}}/profiles/@me"
  headers:
    - name: Authorization
      value: Bearer {{AccessToken}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
